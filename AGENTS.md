# 行为准则
用于减少大语言模型编码常见错误的行为准则。可根据需要与项目专属指令合并使用。
权衡说明：本准则偏向谨慎，而非追求速度。对于简单琐碎任务，请酌情灵活处理。

## 1. 编码前先思考
不要自行臆断，不要掩饰疑惑，主动摆出方案取舍。

在动手实现之前：
- 明确说出你的假设。存在不确定之处，就主动提问。
- 如果存在多种解读方式，全部列出来，不要私自选定某一种。
- 如果有更简单的实现方案，直接说明。必要时可以提出异议。
- 如果有内容理解不清，立刻停下，指出具体疑惑点，进行询问。

## 2. 优先追求简洁
用最少的代码解决问题，不做无依据的猜想式实现。
- 不实现需求之外的功能。
- 仅使用一次的代码，不要做抽象封装。
- 不额外增加未被要求的灵活性、可配置性。
- 不为不可能发生的场景编写错误处理逻辑。
- 如果你写出200行代码，但50行就可以完成，就重写。

自问：“资深工程师会不会觉得这段代码过度复杂？”如果答案是肯定，就做简化。

## 3. 精准局部修改
只改动必须改动的部分，只清理自己造成的问题。

修改已有代码时：
- 不要顺手“优化”周边代码、注释或是代码格式。
- 不去重构没有问题的代码。
- 遵循项目现有的代码风格，即便你习惯的写法不一样。
- 发现无关的废弃代码，只做提示，不要直接删除。

如果你的改动产生了无用遗留代码：
- 删除**因你的修改**而不再使用的导入、变量、函数。
- 除非收到明确要求，否则不要删除原本就存在的废弃代码。

检验标准：每一行改动的代码，都应当可以直接追溯到用户的需求。

## 4. 以目标为导向执行
明确成功判定标准，反复迭代直到验证通过。

把任务转化为可验证的目标：
- “增加校验逻辑” → “编写非法输入的测试用例，再让测试全部通过”
- “修复这个Bug” → “编写可以复现该问题的测试用例，再让测试全部通过”
- “重构X模块” → “保证重构前后所有测试均可正常运行”

多步骤任务，给出简短执行计划：
1. [步骤] → 验证点：[检查项]
2. [步骤] → 验证点：[检查项]
3. [步骤] → 验证点：[检查项]

清晰明确的成功标准，可以让你独立迭代完成工作；模糊的标准（例如“把它弄好”），则需要反复确认需求。

## 5. Git commit提交规范
提交信息使用 Conventional Commits，确保日志可读、便于生成变更记录。

### 提交粒度

- 单次提交只做一类变更（功能/修复/文档）
- 提交前先整理改动，避免混入无关格式化或临时调试
- 每个提交应可构建、可运行，方便回滚
- 大改动拆分为多个可审查的小提交

### 提交信息格式

`<emoji> <type>[(scope)]: <summary>  [body]  [footer]`

- 注意携带Emoji图标，下方类型中有样式。
- type 建议：feat、fix、docs、refactor、test、chore、build（其他场景按需）
- scope 使用模块/目录（如 app、data、scripts），无明确范围可省略
- summary 使用中文、动词开头，长度 ≤ 50 字，不加句号
- 需要时在正文补充动机、影响或迁移方式

### 提交类型

|类型|说明|
|---|---|
|`🎉 init`|项目初始化|
|`✨ feat`|新功能|
|`🐞 fix`|错误修复|
|`📃 docs`|文档变更|
|`🌈 style`|代码格式化（不影响代码逻辑）|
|`🦄 refactor`|代码重构（不新增功能或修复错误）|
|`🎈 perf`|性能优化|
|`🧪 test`|测试相关|
|`🔧 build`|构建系统或外部依赖|
|`🐎 ci`|CI配置相关|
|`🐳 chore`|构建过程或辅助工具的变动|
|`↩ revert`|撤销提交|

### 破坏性变更

- 在 type 后添加 `!`，或在正文写明 `BREAKING CHANGE: ...`
- 明确写出受影响范围与升级指引

###  提交流程

- `git status` 确认改动范围
- `git add <files>` 仅添加相关文件
- `npm run lint` 通过后再提交
- `git commit -m "..."` 完成提交
- `git push` 后发起 PR（如需）