1. 项目背景
当前存在多个边缘服务器，每台边缘服务器部署独立 RustFS，用于本地对象存储。只有一个中控端，需要将多个边缘端 RustFS 中的数据，通过运输硬盘离线拷贝并导入到中控端 RustFS。

多个边缘端分别运行 RustFS。系统通过运输硬盘将边缘端对象离线导出并导入唯一的中控 RustFS，实现可靠、可追溯、可重复执行的单向归档。
数据方向固定为：`边缘端 RustFS → 运输硬盘 → 中控端 RustFS`。


1. 离线磁盘运输目标
- 边缘端自动识别已注册运输硬盘。
- 边缘端自动从本地 RustFS 导出对象数据到运输硬盘。
- 边缘端导出时避免拷贝仍在上传或正在变化的对象。
- 运输硬盘封盘后，中控端自动识别并导入。
- 中控端根据边缘端信息创建或复用对应归档存储桶。
- 中控端导入时支持去重，避免重复导入对象。
- 两端前端页面实时展示硬盘状态、任务状态、进度、速度、当前文件。
- 所有离线交接必须通过统一运输盘协议完成，避免边缘端和中控端协议不一致。

1. 系统范围
3.1 系统范围
- 中控端程序。
- 边缘端程序。
- 中控端前端页面。
- 边缘端前端页面。
- Linux 运输硬盘热插拔识别。
- RustFS S3 对象扫描、下载和上传。
- 运输盘协议文件读写。
- 导出和导入任务进度实时展示。

1. 角色与职责
4.1 中控端
中控端是运输盘注册、导入和最终状态判断的权威端。
中控端需要负责：
- 注册运输硬盘。
- 初始化运输硬盘。
- 维护边缘站点信息。
- 识别插入的运输硬盘。
- 判断运输硬盘是否已注册。
- 判断运输硬盘是否可以初始化、导入或复用。
- 导入已封盘运输硬盘中的数据。
- 将数据写入中控端 RustFS。
- 根据对象账本避免重复导入。
- 导入完成后标记运输硬盘已导入，并触发清理和重新初始化流程。
- 向中控端前端实时推送导入状态。

4.2 边缘端
边缘端负责从本地 RustFS 导出数据，并按协议写入运输硬盘。
边缘端需要负责：
- 配置站点名称、站点编码、中控授权key信息、RustFS S3 信息和数据库信息。
- 识别插入的运输硬盘。
- 校验运输硬盘是否已注册。
- 判断运输硬盘是否可用于本次导出。
- 扫描本地 RustFS 对象。
- 判断对象是否稳定，避免导出正在上传或变化的对象。
- 将待导出对象分配到一个或多个运输硬盘。
- 记录导出任务进度和对象状态。
- 导出完成后封盘。
- 向边缘端前端实时推送拷贝状态。

4.3 运输硬盘
运输硬盘是边缘端和中控端之间的离线数据载体。
运输硬盘需要承载：
- 运输盘身份信息。
- 当前运输盘状态。
- 来源边缘端信息。(用于根据边缘端做存储桶归类)
- 对象数据。
- 对象元数据。
- 对象清单。
- 校验信息。
- 导出和导入日志。