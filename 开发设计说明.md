# RustFS 离线运输盘同步：开发架构与功能清单

## 1. 系统要解决什么

多个边缘端各自运行 RustFS。边缘端将本地对象写入运输硬盘；硬盘送达中控端后，中控端将对象归档到唯一的中控 RustFS。

```text
边缘端 RustFS → 运输硬盘 → 中控端 RustFS
```

首期只解决这条链路的完整闭环：**注册硬盘、边缘端导出、硬盘封盘、中控端导入、导入结果查询**。

## 2. 功能清单

### 2.1 中控端

| 编号 | 功能 | 结果 |
|---|---|---|
| C01 | 运输硬盘管理 | 查看文件系统类型；非 ext4 的硬盘经确认后格式化为 ext4；注册、初始化及导入后的重新初始化 |
| C02 | 边缘站点管理 | 维护站点名称、站点编码（中控归档桶名），并为 Edge 分配固定 Key |
| C03 | 封盘批次识别 | 读取封盘批次，显示来源站点、对象数、容量和状态 |
| C04 | 批次导入 | 将封盘批次对象写入中控 RustFS |
| C05 | 对象去重 | 已成功归档的对象不重复写入 |
| C06 | 导入任务查询 | 展示任务状态、进度、速度、当前对象和错误 |
| C07 | 导入记录 | 查询某块盘、某个批次或某个对象的导入结果 |

### 2.2 边缘端

| 编号 | 功能 | 结果 |
|---|---|---|
| E01 | 站点配置 | 配置站点信息、本地 RustFS 和中控端分配的固定 Key |
| E02 | 可用盘判断 | 判断运输硬盘是否已注册且可用于导出 |
| E03 | 对象扫描 | 通过 S3 接口全量获取本地 RustFS 的对象列表 |
| E04 | 对象导出 | 按硬盘可用容量将对象和对象信息写入运输硬盘 |
| E05 | 封盘 | 当前对象放不下或待导出对象全部完成时，生成不可再修改的对象清单并标记批次完成 |
| E06 | 导出任务查询 | 展示任务状态、进度、速度、当前对象和错误 |
| E07 | 运行时导出账本 | 记录当前封盘批次的对象写入状态；封盘确认后供下一块硬盘筛选未导出对象 |

### 2.3 两端共有

| 编号 | 功能 | 结果 |
|---|---|---|
| S01 | 本地硬盘挂载与目录扫描 | 两端主程序共用同一套逻辑，自动挂载并扫描本机运输硬盘 |
| S02 | 运输硬盘通讯协议（盘上协议） | 两端使用同一目录结构和文件格式交接数据 |
| S03 | 任务执行 | 创建、执行、失败、恢复导出/导入任务 |
| S04 | 状态展示 | 通过 WebSocket 向页面实时推送硬盘和任务状态 |
| S05 | 操作记录 | 保存硬盘、批次、任务的关键操作和错误 |
| S06 | Docker 一键部署 | 使用单条 Docker Compose 命令启动中控端或边缘端 |

## 3. 架构拆分

系统分为中控端、边缘端和运输硬盘三部分。中控端与边缘端不直接传输对象；运输硬盘是唯一的数据交接介质。

```text
共享 Module
└── 本地硬盘挂载与目录扫描 Module
    ├── 中控端实例只管理中控主机的运输硬盘
    └── 边缘端实例只管理边缘主机的运输硬盘

边缘端
├── 管理页面
├── 导出任务 Module
│   ├── 本地 RustFS Module
│   └── 运输盘协议 Module ──→ 运输硬盘
├── 导出账本 Module
└── 边缘端数据库

中控端
├── 管理页面
├── 导入任务 Module
│   ├── 中控 RustFS Module
│   ├── 运输盘协议 Module ←── 运输硬盘
│   └── 对象账本 Module
└── 中控端数据库
```

这里的 Module 是功能归属，而不是先确定代码目录或微服务。每个 Module 对外只暴露完成业务动作所需的 Interface，把设备扫描、文件读写、状态维护等细节留在内部。

## 4. Module 职责与 Interface

### 4.1 本地硬盘挂载与目录扫描 Module

**职责**：由中控端和边缘端主程序共用同一套逻辑，自动挂载本机新插入的运输硬盘，并扫描配置目录发现、移除硬盘。

**输入**：一个或多个挂载目录、扫描间隔。

**输出**：当前已发现的硬盘列表；硬盘发现、移除和内容变更结果。

**边界**：中控端实例只处理本机插入的硬盘，边缘端实例也只处理本机插入的硬盘；不负责开始导出或导入。

### 4.2 运输硬盘通讯协议 Module

**职责**：读写运输硬盘中的身份信息、封盘批次、对象清单和对象数据。

**输入**：运输硬盘根目录、硬盘标识、封盘批次、对象条目。

**输出**：已验证的硬盘信息、批次信息、对象条目和校验结果。

**不负责**：扫描 RustFS、决定哪些对象导出、将对象上传到 RustFS。

### 4.3 边缘端导出任务 Module

**职责**：从本地 RustFS 获取对象，将对象写入指定运输硬盘，并完成封盘。

**输入**：站点配置、运输硬盘、导出范围。

**输出**：封盘批次及导出任务结果。

**不负责**：中控端导入、运输硬盘注册。

### 4.4 导出账本 Module（边缘端）

**职责**：记录当前封盘批次的对象写入状态；仅在封盘完成后将对象标记为已导出，并为后续运输硬盘筛选尚未导出的对象。

**输入**：对象标识（来源桶、来源对象键、ETag、大小）、封盘批次、写入结果、封盘结果。

**输出**：待导出对象列表、当前批次运行时记录、已确认的对象导出记录。

**不负责**：对象扫描、对象复制和中控端去重。

### 4.5 中控端导入任务 Module

**职责**：读取封盘批次，将其中对象归档到中控 RustFS，记录导入结果。

**输入**：运输硬盘、封盘批次。

**输出**：导入任务结果、归档对象记录。

**不负责**：修改封盘批次中的对象数据、从中控端回传边缘端。

### 4.6 对象账本 Module（中控端）

**职责**：保存对象已归档的事实，供导入任务判断是否需要重复写入。

**输入**：来源 Edge 编码、来源桶、来源对象键、ETag、大小、归档位置。

**输出**：对象是否已归档，以及归档记录。

**不负责**：实际上传对象。

### 4.7 任务与状态 Module

**职责**：保存导出/导入任务、进度和错误；向页面提供统一的状态数据。

**输入**：任务创建、进度更新、完成或失败结果。

**输出**：任务详情、任务列表、实时状态事件。

**不负责**：对象复制和协议文件读写。

## 5. 关键业务流程

### 5.1 注册运输硬盘

1. 中控端主程序使用共享挂载逻辑，将本机新插入硬盘自动挂载到已配置目录并发现该硬盘。
2. 中控端读取硬盘文件系统类型。
3. 文件系统不是 ext4 时，页面提示格式化风险；操作员确认后将硬盘格式化为 ext4。
4. 格式化后的硬盘重新挂载到已配置目录。
5. 中控端为硬盘创建唯一标识并写入运输盘身份信息。
6. 中控端保存硬盘登记记录，硬盘状态变为“可导出”。

### 5.2 边缘端导出

1. 扫描器发现运输硬盘。
2. 导出任务读取运输盘身份信息：状态为 `READY` 时创建新批次；状态为 `WRITING` 时继续当前批次。
3. 导出任务通过 S3 接口全量扫描本地 RustFS，仅处理已可见对象；分片上传未完成的对象不在扫描结果中。
4. 导出账本 Module 从扫描结果中筛选未导出对象。
5. 导出任务读取对象元数据，记录 ETag 和大小；来源桶、来源对象键、ETag、大小共同构成对象版本标识。
6. 导出任务计算当前可写空间：硬盘总容量减去已使用空间和预留运输空间；预留运输空间用于协议文件、对象元数据和 ZIP 的额外占用。
7. 当前可写空间不足以容纳“对象大小 + ZIP 预留空间”时，停止写入并封盘。
8. 导出任务将对象和对象条目写入运输硬盘；写入完成后再次读取对象元数据。两次 ETag 或大小不一致时，丢弃本次写入结果并在下一轮导出。
9. 运行时导出账本记录对象已写入当前封盘批次。
10. 当前对象放不下或待导出对象已全部写完时，运输硬盘通讯协议 Module 写入封盘标记。
11. 批次状态变为“已封盘”；运行时导出账本将当前批次对象标记为已确认导出。

### 5.3 中控端导入

1. 扫描器发现运输硬盘。
2. 导入任务读取硬盘身份信息和已封盘批次。
3. 中控端依据运输盘中的来源 Edge 编码确定归档桶：一个 Edge 固定对应一个中控端归档桶，桶名使用 Edge 编码。
4. 中控端保持源存储结构归档：目标对象键为 `来源桶名/来源对象路径`。
5. 针对每个对象条目查询对象账本；未归档对象写入中控 RustFS。
6. 对象写入成功后记录对象账本。
7. 全部完成后，批次状态变为“已导入”。
8. 仅当全部对象均已完成解密、校验、写入中控 RustFS 且对象账本已成功落库时，中控端才自动重新初始化：保留硬盘登记身份，清除上一封盘批次的数据，使硬盘恢复为“可导出”状态。

## 6. 运输硬盘通讯协议的首期范围

首期协议只需要四类内容：

| 内容 | 用途 | 由谁写入 |
|---|---|---|
| 硬盘身份信息 | 标识哪一块已注册运输硬盘 | 中控端初始化时 |
| 批次信息 | 标识来源站点、批次状态和统计信息 | 边缘端创建/封盘时 |
| 对象清单 | 描述每个对象的来源和盘上位置 | 边缘端导出时 |
| 对象数据 | 实际待导入的数据 | 边缘端导出时 |

协议文件的具体文件名、JSON 字段和校验算法见 [运输硬盘通讯协议.md](运输硬盘通讯协议.md)。

### 6.1 ZIP 密码加密

中控端为每个 Edge 分配固定 Key；首期不支持更换。Edge 首次部署时与数据库配置一同提供该 Key。

每个对象以传统 ZIP 密码（ZipCrypto）压缩包写入运输硬盘。ZIP 密码统一按 `站点 Code + 中控端分配的Key` 拼接；同一 Edge 的全部对象使用同一个 ZIP 密码。中控端根据运输盘中的来源 Edge 编码和固定 Key 拼出相同密码后解压归档。
运输硬盘和浏览器不得保存固定 Key 或 ZIP 密码。

## 7. 首期页面

| 页面 | 端 | 首期能力 |
|---|---|---|
| 运输硬盘列表 | 两端 | 显示扫描到的硬盘、标识、状态和容量 |
| 运输硬盘详情 | 两端 | 查看硬盘信息及关联批次 |
| 导出任务 | 边缘端 | 创建导出任务、查看进度和错误 |
| 导入任务 | 中控端 | 启动导入、查看进度和错误 |
| 边缘站点管理 | 中控端 | 维护站点名称、站点编码及 Edge 固定 Key |
| 系统配置 | 边缘端 | 配置站点和本地 RustFS 连接信息 |

## 8. Docker 一键部署

首期交付中控端和边缘端各一套 Docker Compose 部署文件。使用者填写对应的环境变量文件后，执行以下命令即可启动：

```bash
docker compose up -d
```

部署文件必须完成以下事项：

- 启动应用及其必需依赖；
- 通过部署配置指定一个或多个宿主机挂载目录，并映射到容器；
- 中控端和边缘端主程序共用本地硬盘自动挂载逻辑，各自只管理所在主机插入的硬盘；容器必须具备挂载硬盘的宿主机权限；
- 中控端还必须具备读取已挂载硬盘文件系统类型、执行 ext4 格式化和重新挂载的宿主机权限；
- 将数据库数据、应用配置和日志映射为持久化目录；
- 通过部署配置提供站点信息、RustFS 连接信息和 Edge 固定 Key；
- 提供中控端与边缘端各自独立的示例环境变量文件。

## 9. 开发顺序

按可闭环验证的顺序交付：

1. 定义运输盘协议最小字段，并实现协议读写 Module。
2. 实现两端共用的本地硬盘挂载与目录扫描 Module 和运输硬盘列表页面。
3. 实现中控端硬盘注册与初始化。
4. 实现边缘端对象扫描、导出和封盘。
5. 实现中控端批次识别、导入和对象账本。
6. 实现任务进度和页面实时刷新。
7. 补充失败恢复、校验、权限和审计。
8. 提供中控端、边缘端的 Docker Compose 部署文件和部署说明。
