# 运输硬盘通讯协议 v1

## 1. 目标

边缘端通过运输硬盘交接一个封盘批次给中控端。

- 一块运输硬盘同一时刻只承载一个封盘批次；
- 一个封盘批次只来自一个 Edge；
- 中控端只导入已封盘批次；
- 不使用 KDF、AAD、签名、HMAC 或恢复索引；
- ZIP 密码格式固定为传统 ZIP 密码（ZipCrypto）；
- 运输硬盘只使用两个 JSON 文件。

## 2. 目录结构

```text
rustfs-transport/
├── disk_info.json     # 运输盘身份、生命周期状态、封盘信息
├── manifest.jsonl     # 每行一个对象
└── data/              # ZIP 密码压缩包
    ├── 00000001.zip
    ├── 00000002.zip
    └── ...
```

中控端重新初始化时保留 `disk_info.json` 中的硬盘身份，清空批次信息；删除 `manifest.jsonl` 和 `data/`。

## 3. 通用规则

- JSON 使用 UTF-8；时间使用 UTC 的 RFC 3339 格式；
- 所有路径均相对 `rustfs-transport/`；
- 每个对象先写入临时文件，完整写入后原子改名为 `.zip`，再追加清单条目；
- `disk_info.json` 的状态更新为 `SEALED` 必须最后执行；状态不是 `SEALED` 时中控端不得导入；
- Edge 编码用于中控端选择归档桶，并与固定 Key 拼接为 ZIP 密码。

## 4. 文件定义

### 4.1 `disk_info.json`

中控端初始化运输硬盘时创建，保存硬盘身份、当前状态和当前封盘批次信息。

```json
{
  "protocol_version": 1,
  "disk_id": "disk-001",
  "state": "READY",
  "initialized_at": "2026-08-20T08:00:00Z",
  "batch": null
}
```

边缘端开始导出时将状态更新为 `WRITING` 并写入 `batch_id`、`edge_code`、`created_at`。封盘时更新为 `SEALED`，并写入 `manifest_sha256`、`object_count` 和 `sealed_at`。

### 4.2 `manifest.jsonl`

每一行是一个已完整写入硬盘的对象：

```json
{"type":"object","sequence":1,"source_bucket":"source-bucket","source_key":"2026/08/a.mp4","etag":"abc123","size":1073741824,"plain_sha256":"hex","zip_path":"data/00000001.zip"}
```

对象版本标识为：`source_bucket + source_key + etag + size`。

## 5. 边缘端写盘流程

1. 读取 `disk_info.json`，确认运输硬盘已注册。
2. 状态为 `READY` 时创建批次、将状态更新为 `WRITING` 并创建 `manifest.jsonl`；状态为 `WRITING` 时读取已有 `manifest.jsonl`，从未写入清单的对象继续导出。
3. 全量扫描 S3，并由导出账本筛选未导出对象。
4. 对每个对象：读取 ETag 和大小；使用 `站点 Code + 固定 Key` 作为密码创建 ZIP 压缩包并写入 `data/`；追加对象条目；记录运行时导出账本。
5. 当前对象放不下或待导出对象全部完成时，计算 `manifest.jsonl` 的 SHA256，并将 `disk_info.json` 更新为 `SEALED`。
6. 将运行时导出账本中的当前批次对象标记为已确认导出。

## 6. 中控端导入流程

1. 读取 `disk_info.json` 和 `manifest.jsonl`。
2. 校验硬盘已注册、状态为 `SEALED`，且批次信息中的清单 SHA256 与实际清单一致。
3. 从 `disk_info.json` 取得 `edge_code`，定位中控归档桶和固定 Key，并拼出 ZIP 密码。
4. 逐条读取对象清单：查询对象账本；未归档对象读取 ZIP 压缩包，使用密码解压后校验 `plain_sha256`，写入中控 RustFS。目标对象键为 `source_bucket/source_key`，保持源存储结构。
5. 成功写入后记录对象账本。
6. 仅在全部清单条目均完成后，自动重新初始化运输硬盘。

## 7. 本协议不处理的事项

- 运输硬盘的自动挂载、格式化和卸载；
- Edge 固定 Key 的首次部署配置；
- WebSocket 状态推送；
- 对蓄意修改清单内容的防护。
